part of '../_index.dart';

class NeedLoginDelta extends StatelessWidget {
  const NeedLoginDelta({Key? key}) : super(key: key);

  NeedLoginCtrl get ct => x1NeedLoginCtrl;
  NeedLoginData get dt => x1NeedLoginData.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
