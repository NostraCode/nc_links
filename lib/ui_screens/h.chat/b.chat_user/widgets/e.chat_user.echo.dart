part of '../_index.dart';

class ChatUserEcho extends StatelessWidget {
  const ChatUserEcho({Key? key}) : super(key: key);

  ChatUserCtrl get ct => Ctrl.chatUser;
  ChatUserData get dt => Data.chatUser.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
