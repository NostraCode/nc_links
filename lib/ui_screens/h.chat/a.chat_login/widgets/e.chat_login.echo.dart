part of '../_index.dart';

class ChatLoginEcho extends StatelessWidget {
  const ChatLoginEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
