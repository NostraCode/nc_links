part of '../_index.dart';

class RestDetailCharlie extends StatelessWidget {
  const RestDetailCharlie({Key? key}) : super(key: key);

  RestDetailCtrl get ct => Ctrl.restDetail;
  RestDetailData get dt => Data.restDetail.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
