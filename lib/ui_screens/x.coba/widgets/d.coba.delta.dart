part of '../_index.dart';

class CobaDelta extends StatelessWidget {
  const CobaDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
