part of '../_index.dart';

class AppCheckDelta extends StatelessWidget {
  const AppCheckDelta({Key? key}) : super(key: key);

  AppCheckCtrl get ct => Ctrl.appCheck;
  AppCheckData get dt => Data.appCheck.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
