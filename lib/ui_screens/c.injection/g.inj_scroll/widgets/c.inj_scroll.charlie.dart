part of '../_index.dart';

class InjScrollCharlie extends StatelessWidget {
  const InjScrollCharlie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
