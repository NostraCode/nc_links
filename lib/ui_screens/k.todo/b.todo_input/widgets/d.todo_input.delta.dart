part of '../_index.dart';

class TodoInputDelta extends StatelessWidget {
  const TodoInputDelta({Key? key}) : super(key: key);

  TodoInputCtrl get ct => Ctrl.todoInput;
  TodoInputData get dt => Data.todoInput.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
