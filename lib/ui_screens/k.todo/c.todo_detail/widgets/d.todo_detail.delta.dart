part of '../_index.dart';

class TodoDetailDelta extends StatelessWidget {
  const TodoDetailDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
