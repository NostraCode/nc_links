part of '../_index.dart';

class TodoDetailEcho extends StatelessWidget {
  const TodoDetailEcho({Key? key}) : super(key: key);

  TodoDetailCtrl get ct => Ctrl.todoDetail;
  TodoDetailData get dt => Data.todoDetail.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
