part of '../_index.dart';

class TodoEditDelta extends StatelessWidget {
  const TodoEditDelta({Key? key}) : super(key: key);

  TodoEditCtrl get ct => Ctrl.todoEdit;
  TodoEditData get dt => Data.todoEdit.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
