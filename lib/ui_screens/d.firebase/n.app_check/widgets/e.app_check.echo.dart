part of '../_index.dart';

class AppCheckEcho extends StatelessWidget {
  const AppCheckEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
