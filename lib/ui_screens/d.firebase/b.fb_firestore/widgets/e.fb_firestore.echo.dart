part of '../_index.dart';

class FbFirestoreE extends StatelessWidget {
  const FbFirestoreE({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
