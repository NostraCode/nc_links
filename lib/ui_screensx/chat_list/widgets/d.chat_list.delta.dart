part of '../_index.dart';

class ChatListDelta extends StatelessWidget {
  const ChatListDelta({Key? key}) : super(key: key);

  ChatListCtrl get ct => x1ChatListCtrl;
  ChatListData get dt => x1ChatListData.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
