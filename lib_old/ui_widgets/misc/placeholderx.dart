part of '_index.dart';

class Placeholderx extends StatelessWidget {
  const Placeholderx({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      strokeWidth: 0.6,
      color: Colors.grey,
    );
  }
}
