part of '../_index.dart';

class ProductDetailE extends StatelessWidget {
  const ProductDetailE({Key? key}) : super(key: key);

  ProductDetailCtrl get ct => Ctrl.productDetail;
  ProductDetailData get dt => Data.productDetail.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
